# Use the crate module path instead of a file path
rust_input: "crate::api"

# Explicitly define where your Rust project lives
rust_root: "rust/"

# Define where the generated Dart code should go
dart_output: "lib/src/rust"